import Workspace from '@layouts/Workspace';
import React from 'react';

const DirectMessage = () => {
    return (
        <Workspace>
            <div>로그인ㅊㅊ</div>
        </Workspace>
    );
};

export default DirectMessage;
